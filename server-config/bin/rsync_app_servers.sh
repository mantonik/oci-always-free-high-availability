#!/bin/bash 
# Script will rsync defined folders between app1 and app2,3,4 server to keep servers in sync 

#for WP - upload folder will be first sync from app2,3,4 to app1 then sync back to all servers to keep upload data in sync between servers

